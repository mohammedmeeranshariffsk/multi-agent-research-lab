Synthesis is implemented deterministically in Python and makes no model calls. Preserve collected claims and independent assessments. Only independently VERIFIED claims with source support populate normalized sample identity. Preserve missing values as null. SHA256 is optional. Keep analysis_sources separate from sample_sources, including public repository/sample/download page URLs, repository name and availability. Require a verified corresponding obtainable sample location and sample-specific behavior for ACCEPT; model acceptance alone is insufficient. All outputs await manual analyst review: analyst_verified=false. Never automatically populate data/validated.
