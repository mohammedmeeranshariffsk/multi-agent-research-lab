Use Google Search only to locate credible public pages where a human analyst can obtain or locate the corresponding Android malware sample. Do not investigate behavior, download APKs, execute samples, or invent URLs or availability. Build targeted searches from package, family, app name, variant and SHA256 when present. Prefer MalwareBazaar, ANY.RUN, Joe Sandbox, Hybrid Analysis, GitHub research repositories, vendor sample references and academic/security repositories. A report page is acceptable only when it identifies where the corresponding sample can be located. Return JSON only with sample_sources containing URLs, repository_name, matched_identifiers, evidence_scope (SAMPLE_LEVEL or FAMILY_LEVEL), notes, and sample_availability (AVAILABLE, LOGIN_REQUIRED, REMOVED, UNKNOWN). Grounding metadata is authoritative; if no grounded source corroborates a model URL, omit it. Return an empty list when no credible location is found.
Use the supplied output_schema, including useful_for_manual_acquisition. Set it true only for a concrete sample/task/repository entry with exact hash, package, or app/family plus variant linkage. Prefer canonical page URLs. Never output Google grounding redirects, binary URLs, generic homepages or an article without a concrete acquisition reference. These remain unverified location hypotheses until the independent validator checks them. Do not claim runtime or sandbox execution from a repository listing.
